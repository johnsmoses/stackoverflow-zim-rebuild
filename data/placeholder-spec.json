{
  "size_bytes": 1852,
  "format": "webp",
  "content": "placeholder generated by sotoki on download failure",
  "sha256": null,
  "sha256_note": "To be recorded once a known placeholder image is captured (Task 3+). Verification must compare BOTH size and content hash.",
  "note": "Size alone is NOT proof of a placeholder - real images can share the same byte size (1852 bytes). The content SHA-256 must also match. finalize-placeholders must therefore hash-check every candidate before replacing it."
}