# Environment / secrets — never commit
.env
.env.*
!.env.example
secrets/
*private-key*
*private*key*
*.key

# WireGuard configs (tunnel used for CDN image recovery) — operator-managed
# and NEVER committed; the repo contains no interface config or keys
*.wg.conf
wg*.conf
wg0.conf
wg3.conf
.wg*

# Working data — never commit
data/stage/
data/redis/
data/zim/
data/assets/
data/baseline-bundle/
*.zim
*.rdb
*.7z
*.sqlite
*.log

# Patch work-in-progress
*.patch.tmp

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# Node (unused for now, kept for future tooling)
node_modules/
# Stage-1 forensic capture evidence
capture/

# Docker image worker working data — never commit
# (fixture manifests, SQLite checkpoints, recovered images, IA shard mounts)
docker/image-worker/manifests/
docker/image-worker/state/
docker/image-worker/recovered/
docker/image-worker/ia-shards/
